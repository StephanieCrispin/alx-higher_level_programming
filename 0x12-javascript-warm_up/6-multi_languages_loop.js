#!/usr/bin/node
// Loops through and prints out array items

const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
let i;
for (i = 0; i < array.length; i++) {
  console.log(array[i]);
}
